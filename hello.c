#include<stdlib.h>
#include<stdio.h>
#include<math.h> //sqrt and powers etc., 

// copies and pastes another text file into the program

int main()
{
	int x; // declaration

	x = 7; // initialization

	int y = 5; // both together


	int z = x+y;

	printf("Hello World!! %d + %d = %d",x,y,z);

	return 0;

	// in gnu editor
	// gcc the name of the text file. (make sure in same directory)
	// it should create an exe file 
	// then you can run executable with ./a.exe

}

// this is the change
